#include <string.h>


void strappend(char* src,char* append,char* dst){
     strcpy(dst,src);
     int len = strlen(src);
     strcpy(dst + len,append);
     char* str = new char[100];
     strcpy(str,"hahahhahah");
}
