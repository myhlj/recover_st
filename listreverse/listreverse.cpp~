#include <iostream>
#include <list>
using namespace std;

int main(){
    list<int> one;
    one.push_back(1);
    one.push_back(2);
    one.push_back(3);
    one.push_back(4);
    one.push_back(5);
    one.push_back(6);

    one.reverse();
    return 0;
}
